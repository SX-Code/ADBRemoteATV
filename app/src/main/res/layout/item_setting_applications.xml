<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@color/btn_bg_color"
    android:padding="5dp">

    <ImageButton
        android:id="@+id/btn_setting_app_drag"
        android:layout_width="30dp"
        android:layout_height="30dp"
        android:padding="4dp"
        android:scaleType="fitCenter"
        android:contentDescription="@string/text_drag"
        android:src="@drawable/ic_drag"
        android:background="@android:color/transparent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:tint="@color/text_color" />

    <ImageView
        android:id="@+id/iv_setting_app_icon"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:padding="4dp"
        android:scaleType="fitCenter"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toEndOf="@id/btn_setting_app_drag"
        app:layout_constraintTop_toTopOf="parent" />


    <TextView
        android:id="@+id/iv_setting_app_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="10dp"
        android:textColor="@color/text_color"
        android:textSize="16sp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toEndOf="@+id/iv_setting_app_icon"
        app:layout_constraintTop_toTopOf="parent" />


    <ImageButton
        android:id="@+id/btn_setting_delete_app"
        android:layout_width="30dp"
        android:layout_height="30dp"
        android:layout_marginEnd="8dp"
        android:contentDescription="@string/text_remove"
        android:background="@drawable/background_btn_circle_tr"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toStartOf="@id/btn_setting_edit_app"
        app:layout_constraintTop_toTopOf="parent"
        app:srcCompat="@drawable/ic_close_circle"
        app:tint="@color/error_color"/>

    <ImageButton
        android:id="@+id/btn_setting_edit_app"
        android:layout_width="30dp"
        android:layout_height="30dp"
        android:layout_marginEnd="8dp"
        android:contentDescription="@string/text_edit"
        android:background="@drawable/background_btn_circle_tr"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:srcCompat="@drawable/ic_pen_circle"
        app:tint="@color/text_color" />

</androidx.constraintlayout.widget.ConstraintLayout>